sweep:
  delays_ms: [0]

  traces:
    - name: constant
      up:   ../traces/Constant.up
      down: ../traces/Constant.down

    # - name: bursty
    #   up:   ../traces/Bursty.up
    #   down: ../traces/Bursty.down

    # - name: ramp
    #   up:   ../traces/Ramp.up
    #   down: ../traces/Ramp.down
